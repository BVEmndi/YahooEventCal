url=https://uk.yahoo.com/
url1=https://uk.yahoo.com/?p=us
browser= Chrome
#remote webdriver is used when the browser and the java executor are in different machines,
# remote webdriver is superclass of firefox driver,chrome driver where we can change the
# IP address of the target selenium server.
#incase of chrome drive same remote webdriver is invoked however inlocal environment implicitly
# in remote webdriver we have to set the capabilities including the browser name as a  mandatory parameter and followed
# optional parameter like browser version,platform,https links,class
#java -jar selenium-server-standalone-3.154.0 jar -role node -hub http://localhost:

